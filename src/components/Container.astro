<div class="container flex">
  <slot />
</div>

<style>
  .container {
    margin-inline: 1rem;
    flex-direction: column;
  }

  @media (min-width: 35em) {
    .container {
      margin-inline: auto;
      max-width: min(80vw, 60rem);
    }
  }
</style>
